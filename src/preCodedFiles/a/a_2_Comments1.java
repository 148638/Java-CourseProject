package preCodedFiles.a;

public class a_2_Comments1 {
    // Use the slash symbol (/) twice for a one lined comments
    /*
    Use the slash symbol (/) first, and the star symbol (*) second, to signify the start of a bigger comment
    Use the star symbol (*) first, and the slash symbol (/) second, to signify the end of a bigger comment
     */
}
